<h1>新規登録</h1>
<h4>アカウントをお持ちでない方がユーザーの新規登録を行います。</h4>
<div class="sub flame">
  <%= form_with(model: @user, local: true) do |f| %>
    <%= render 'shared/error_messages', model: f.object %>
    
    <div class="form-group user-dayo">
      <%= f.label :ユーザー名 %><span class="must"> (必須)</span>
        <%= f.text_field :name, class: "form-control", placeholder:"ユーザー名を入力してください。", autocomplete:"off" %>	
    </div>
    
    <div class="form-group">
      <%= f.label :メールアドレス %><span class="must"> (必須)</span>
        <%= f.email_field :email, class: "form-control", placeholder:"メールアドレスを入力してください。", autocomplete:"off" %>	
    </div>
    
    <div class="form-group">
      <%= f.label :パスワード %><span class="must"> (必須)</span>
        <%= f.text_field :password, class: "form-control", placeholder:"パスワードを入力してください。(6字以上)", type:"password" %>	
    </div>
    
    <div class="form-group">
      <%= f.label :パスワード確認用 %><span class="must"> (必須)</span>
        <%= f.text_field :password_confirmation, class: "form-control", placeholder:"確認用のパスワードを入力してください。", type:"password" %>	
    </div>

    <div class="form-group">
      <%= f.label :プロフィール画像 %>
        <%= f.file_field :user_image, class: "form-control", placeholder:"プロフィール画像を選択してください。（任意）" %>
    </div>

    <%= f.submit "登録", class:"btn btn-primary btn-block" %>
  <% end %>
  <div class="already">既にアカウントをお持ちの方は
  <%= link_to "こちら", "#" %>
  </div>
</div>