<% if @tag_posts.present? %>
  <h1><span><%= @tag %></span> に関する投稿</h1>
  <%= render "tag_posts" %>
  <%= paginate @tag_posts %>
<% else %>
  <h1>全ての作品</h1>
  <%= render "shared/posts" %>
  <%= paginate @posts %>
<% end %>